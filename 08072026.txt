Verified Flow
Your flow is mostly correct, with two corrections:
The “internal node map” is a Python dict, so yes, functionally it is a hash map keyed by nodeId.
Node execution is not async in these four files. The FastAPI request is async and the full pipeline is offloaded to a ProcessPoolExecutor, but linkedlist_traverse.py executes nodes sequentially in a while loop.
Actual intended flow:
POST /ss/json -> parse request body -> pipeline JSON/dict -> CreateNodeStructure.json_setting_to_map() -> json_dict node map -> LinkedList.linked_list_creation() -> execution linked list -> linked_list_traversal() -> stop before sink -> execute sink in api_main.py -> upload logs/status -> response.
Important caveat: this local api_main.py does not currently compile because of malformed imports like LOGS JSON at [api_main.py (line 13)](C:/Users/OMEN/Documents/Newgen_DSS_files/api_main.py:13). The other three files pass parse-only compile.
api_main.py
Summary:
Defines the FastAPI app and /ss/json endpoint in create().
Reads raw request bytes through parse_body() and converts them into a Python dict.
Uses ProcessPoolExecutor so each pipeline runs in a subprocess.
run_pipeline() handles logging, Postgres setup, kill-api metadata, node map creation, linked-list creation, traversal, sink execution, uploads, cleanup, and final response.
Calls CreateNodeStructure, LinkedList, linked_list_traversal, and MainControllerRunner.executioner() as the main orchestration chain.
Pros:
Good central orchestration point; subprocess isolation helps with memory cleanup; logging/status upload is integrated.
Cons:
Does not compile in this snapshot; JSON parsing uses string replacement plus ast.literal_eval; node execution is serial; global parser/controller objects may leak state across reused worker tasks; typo SUCESS_VALUE should likely be SUCCESS_VALUE.
Improvements:
Fix malformed imports/constants, use json.loads, instantiate request-scoped parser/controller state, avoid private _pipeline_pool._broken, and move cleanup into safer finally blocks.
JsonParser.py
Summary:
Main class is CreateNodeStructure.
json_setting_to_map() builds final_output, a node-id keyed Python dict/hash map.
Specialized helpers normalize source, sink, feature, model, validation, AutoDex, GenAI, DEX, and Jupyter settings.
It injects shared metadata like pipeline id, timestamp, version, user id, active directory, label column, categorical columns, and sink settings.
create_graph_node() and create_key() build a human-readable pipeline_graph; is_sink_available() detects sink nodes.
Pros:
Centralizes JSON normalization; gives O(1) node lookup by id; handles many node categories.
Cons:
Many except Exception: pass blocks hide bad/missing JSON; mutates input JSON in place; several constants used in create_key() appear undefined/import-missing; parser stores mutable state like common_sink_settings.
Improvements:
Use explicit schema validation, specific exceptions with logging, a stateless parser return object, shared helper for common metadata injection, and properly imported constants.
link_list.py
Summary:
Defines Node, which stores id, json, next_id, and previous_id.
LinkedList.create_list() converts comma-separated node ids into shared Node objects using a dict cache.
linked_list_creation() reads linkNodeOfNAIComponents, links previous/next nodes, chooses a head, builds pipeline_graph, moves sink to the end, and optionally appends a Drools rule node.
update_head_node() tries to adjust the head toward the earliest source node.
rearrange() linearizes the graph into the execution chain consumed by traversal.
Pros:
Maintains object identity per node id; supports prev/next graph metadata; separates sink execution to the end.
Cons:
CreateNodeStructure.create_graph_node() is called as an unbound class method, which should fail at runtime; DAG branches are flattened into one linked list, so parallel/branch semantics may be lost; next_id changes shape from list to single node after rearrange.
Improvements:
Instantiate or inject CreateNodeStructure, return self.headval after head updates, use sets for visited nodes, validate missing/cyclic nodes, and consider a topological DAG executor instead of destructive linked-list flattening.
linkedlist_traverse.py
Summary:
linked_list_traversal() walks the linked list node by node.
It stops when it reaches the sink, returns execution context, and lets api_main.py execute the sink.
For each non-sink node it calls upper_layer_object.executioner() and then object_creator().
It updates status_dict, list_of_nodes_executed, global_dataframe, model_sequence, and serving sequence state.
Helpers manage serving sequence, dataframe cleanup, AutoML categorical column updates, and GenAI-specific dictionary updates.
Pros:
Simple sequential executor; keeps shared execution state in one place; attempts memory cleanup by deleting previous dataframes.
Cons:
Critical typo: serving_seqounce is used but never initialized from serving_sequence; *args makes the function contract fragile; response keys are assumed without validation; failures inside node execution are not locally wrapped with node context.
Improvements:
Replace *args with a typed signature or context object, fix serving_sequence spelling everywhere, validate executor responses, log node-level failures, and only delete prior dataframes when downstream branch dependencies are definitely complete.



so when we say the fastapi request is asynchronous that means every pipeline within the fastapi request is async or that every request sent is async? explain using services from within api_main.py that we have 




6:49 AM






In this api_main.py, “FastAPI request is asynchronous” means the HTTP request handler can yield control while waiting, not that every node inside the pipeline runs asynchronously.
Specifically:
[api_main.py (line 213)](C:/Users/OMEN/Documents/Newgen_DSS_files/api_main.py:213) defines:
@app.post("/ss/json")
async def create(...)
So each /ss/json request is handled by an async FastAPI endpoint. Inside it, two async-ish things happen:
data: bytes = await request.body()
and later:
result = await loop.run_in_executor(pool, run_pipeline, data, log_file)
That second line is the key. FastAPI receives the request, parses it, then submits run_pipeline() to a separate process through ProcessPoolExecutor. While that pipeline is running, the FastAPI event loop is free to handle other incoming requests instead of being blocked.
But the pipeline itself is not async.
Inside run_pipeline() the flow is synchronous:
json_dict = json_parser_object.json_setting_to_map(data)
ll = LinkedList().linked_list_creation(...)
recursion_results = linkedlist_traverse.linked_list_traversal(...)
Then inside linked_list_traversal(), nodes execute one after another in a normal while loop:
while head_node != None:
    setting_url = upper_layer_object.executioner(linked_list_json)
    response = setting_url.object_creator(dictionary_pass)
    head_node = head_node.next_id
So the node flow is:
node 1 executes -> response comes back -> dataframe/status updated -> node 2 executes -> ...
No await, no asyncio.gather, no parallel execution of pipeline nodes.
Also, this pool is configured as:
ProcessPoolExecutor(max_workers=1, ...)
That means only one pipeline process runs at a time in this app instance. If two /ss/json requests arrive:
Request A enters create().
Request A submits run_pipeline() to the process pool.
Request B can still be accepted by FastAPI.
But Request B’s pipeline waits because max_workers=1.
When Request A finishes, Request B’s run_pipeline() starts.
So the clean answer is:
Every request is handled asynchronously at the FastAPI layer, but each pipeline runs synchronously inside run_pipeline(), and node execution is sequential. With max_workers=1, multiple pipeline requests can be accepted concurrently, but actual pipeline execution is queued one at a time.

6:49 AM






in api_main.py, what is the advantage of having each pipeline executed as a separate sub process through process pool executor?




7:14 AM






In api_main.py, the advantage is mainly isolation and memory control.
The endpoint does this:
result = await loop.run_in_executor(pool, run_pipeline, data, log_file)
So the FastAPI request hands the heavy pipeline work to run_pipeline() inside a separate worker process created by ProcessPoolExecutor.
The benefits are:
FastAPI server stays responsive
The pipeline can do CPU-heavy ML/dataframe work without blocking the main FastAPI event loop. Other HTTP requests can still be accepted while one pipeline is running.

Memory is better isolated
ML pipelines often create large dataframes, models, temp objects, feature maps, etc. Running them in a subprocess means much of that memory belongs to the worker process, not the web server process.

Memory can be reclaimed more reliably
The comment in api_main.py says this directly: subprocess exit helps free memory. Python sometimes does not immediately return large memory allocations to the OS inside a long-running process, especially with pandas/numpy/sklearn workloads.

Pipeline crashes are contained
If a pipeline crashes hard or is killed through the kill API, it is less likely to bring down the FastAPI server itself. The code catches BrokenProcessPool and recreates the pool.

Kill API can target the pipeline process
run_pipeline() captures:

process_id = os.getpid()
and stores it through:
dump_kill_api_data_postgresql(...)
That makes it possible for another service/API to identify and terminate the actual pipeline process.
Avoids GIL limitations for CPU-heavy work
Since it uses processes, not threads, CPU-heavy Python/ML work can run outside the main interpreter’s GIL. In this file max_workers=1, so it is not using parallel pipeline execution, but it still gets process isolation.
One nuance: because the pool is configured with:
max_workers=1
this does not mean multiple pipelines execute at the same time in this app instance. It means multiple requests can be accepted, but pipeline execution is queued one at a time. The design seems optimized more for controlled resource usage, crash isolation, killability, and memory cleanup than for high parallel throughput.



10 july, dag based workflow:

                    Frontend JSON
                           │
                           ▼
                      JSON Parser
                           │
                           ▼
                     Build DAG Graph
                           │
                           ▼
                 Compute Initial Indegrees
                           │
                           ▼
                     Initialize State
                    (PipelineState)
                           │
                           ▼
                  Initialize Ready Queue
                (nodes with indegree = 0)
                           │
                           ▼
                 ┌────────────────────┐
                 │ Pipeline Scheduler │
                 └────────────────────┘
                           │
          ┌────────────────┴────────────────┐
          │                                 │
          ▼                                 ▼
    Guardrail Pass                  Guardrail Fail
          │                                 │
          ▼                                 │
   Dispatch to Worker               Remain Ready
 (existing process pool)             but blocked
          │                                 │
          ▼                                 │
     Execute Node                           │
          │                                 │
          ▼                                 │
   Update PipelineState                     │
          │                                 │
          ▼                                 │
 Decrement Child Indegrees                  │
          │                                 │
          ▼                                 │
 Newly Ready Nodes ─────────────────────────┘
          │
          ▼
     Ready Queue




Execution workflow

                  1 (Customer CSV)          2 (Transaction CSV)
                       │                          │
                       │                          │
                       ▼                          ▼
                 3 (Clean Customer)       4 (Clean Transactions)
                       │                          │
                       │                          │
                       └──────────┐    ┌──────────┘
                                  ▼    ▼
                           5 (Join Customer + Transactions)
                             /                      \
                            /                        \
                           ▼                          ▼
              6 (Feature Engineering)      7 (Fraud Features)
                      \                         /
                       \                       /
                        ▼                     ▼
                      8 (Random Forest Model)
                               │
                               ▼
                           9 (Sink)


Explanation of execution of above workflow

Initial State
The scheduler first computes indegrees.
Node
Indegree
1
0
2
0
3
1
4
1
5
2
6
1
7
1
8
2
9
1
Ready Queue initially:
[1, 2]
Both are head nodes.
Scheduler - Iteration 1
Ready Queue
1
2
They are
independent
different input datasets
no shared resources
Guardrails pass.
Scheduler dispatches both simultaneously.
Worker A → Node 1

Worker B → Node 2
Meanwhile
PipelineState

customer_df = None

transaction_df = None
Node 1 finishes first
Node 1 updates the PipelineState object.
state.customer_df = customer_dataframe
Scheduler decrements indegree.
Node 3

1 → 0
Ready Queue becomes
3
Can Node 3 execute?
Yes.
Worker A immediately starts Node 3.
Notice that the scheduler does not wait for Node 2 to finish.
This is an important optimization.
Node 2 finishes
PipelineState
state.transaction_df = transaction_dataframe
Indegree
Node 4

1 → 0
Ready Queue
4
Worker B starts Node 4.
Node 3 and Node 4
Suppose
Node 3
Clean Customer
writes
Customer_Name
Age
Gender
Node 4
Clean Transactions
writes
Amount
Merchant
Date
No overlap.
They execute together.
Completion
After both finish
PipelineState contains
customer_df

transaction_df

clean_customer_df

clean_transaction_df
Node 5
Node 5 depends on BOTH.
Indegree
Initially
2
After Node 3
1
Still blocked.
After Node 4
0
Scheduler pushes
5
to Ready Queue.
Node 5
Joins
clean_customer_df

+

clean_transaction_df
Creates
joined_df
Updates
state.joined_df
Node 6 and Node 7
Now
joined_df
feeds
6

Feature Engineering
and
7

Fraud Feature Engineering
Ready Queue
6

7
Case A
Different columns.
Node 6 writes
Age_Bucket

Income_Band
Node 7 writes
Fraud_Score

Merchant_Risk
No conflicts.
Scheduler dispatches both.
Case B
Conflict
Suppose
Node 6
writes
Amount
Node 7
also writes
Amount
Guardrail detects
Write Amount

Write Amount
Conflict.
Scheduler chooses
Node 6
Node 7 remains
Ready

but blocked
It stays in the Ready Queue.
Node 6 finishes
PipelineState updated.
Scheduler checks.
Node 7
still has
indegree = 0
and now
no conflict
Worker executes Node 7.
Node 8
Random Forest
Depends on
6

7
Initially
indegree = 2
After
Node 6
1
Still blocked.
After
Node 7
0
Scheduler pushes
8
Ready Queue
8
Execute.
Node 9
Sink
Runs after
Node 8
Completes.
PipelineState evolution
Initially
state

├── customer_df = None
├── transaction_df = None
├── joined_df = None
├── model = None
After Node 1
customer_df
After Node 2
transaction_df
After Node 5
joined_df
After Node 8
model = RandomForest(...)
After Node 9
predictions

metrics

artifacts
Why this architecture is robust:

This example demonstrates all the important cases:
Multiple head nodes: Nodes 1 and 2 are both initial sources with indegree 0, so they are immediately eligible for execution.
Independent parallelism: Nodes 1 & 2, and later 3 & 4, can run concurrently because they don't depend on each other.
Dependency synchronization: Node 5 waits until both upstream branches complete before becoming ready.
Guardrail-based scheduling: Nodes 6 & 7 may both be ready, but the scheduler can serialize them if they would conflict (e.g., both writing the same column), while still allowing parallel execution when they operate on disjoint data.
PipelineState updates: Every completed node mutates the same PipelineState object for that pipeline, and those updates are what downstream nodes consume.